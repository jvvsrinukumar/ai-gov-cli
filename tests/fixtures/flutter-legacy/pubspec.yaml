name: legacy_app
description: A Flutter app with legacy MVC structure alongside clean arch features
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.0
  bloc: ^8.1.0
  dio: ^5.3.0
