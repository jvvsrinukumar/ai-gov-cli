import type { GovernanceConfig } from '../../../types.js';

export function generateHooksReadme(c: GovernanceConfig): string {
  return `# Kiro Hooks — ${c.project.appName}

> Generated by ai-gov v${c.hookVersion}. These JSON hook files are read by Kiro automatically.

## Hook Files

| File | Event | Action | Purpose |
|------|-------|--------|---------|
| \`block-dangerous-commands.json\` | preToolUse (shell) | askAgent | Blocks force push, destructive git ops |
| \`protect-files.json\` | preToolUse (write) | askAgent | Warns on high-risk file edits |
| \`pre-write-secrets-gate.json\` | preToolUse (write) | askAgent | Blocks writes containing hardcoded credentials |
${c.specFirstEnabled ? '| `spec-first-gate.json` | preToolUse (write) | askAgent | Blocks writes without spec |\n' : ''}| \`check-secrets.json\` | fileEdited | askAgent | Scans for hardcoded credentials (post-hoc) |
| \`check-file-size.json\` | postToolUse (write) | askAgent | Warns on files > 200 lines |
| \`check-feature-readme.json\` | postToolUse (write) | askAgent | Reminds to update feature README |
| \`check-consistency.json\` | postToolUse (write) | askAgent | Detects spec-vs-code drift |
| \`format-code.json\` | postToolUse (write) | runCommand | Auto-formats after write |
| \`analyze-code.json\` | postToolUse (write) | runCommand | Runs linter after write |
| \`session-continuity.json\` | promptSubmit | askAgent | Context preservation |
| \`require-task-type.json\` | promptSubmit | askAgent | Task classification |
| \`post-task-checklist.json\` | postTaskExecution | askAgent | Post-task verification |

## How Kiro Hooks Work

- **preToolUse** hooks fire before a tool executes. If the hook responds with "DENIED", the tool call is blocked.
- **postToolUse** hooks fire after a tool executes. They can run commands or prompt the agent.
- **fileEdited** hooks fire when a file is saved. Used for post-hoc checks like credential scanning.
- **promptSubmit** hooks fire when a message is sent. Used for session context and task classification.
- **postTaskExecution** hooks fire after a spec task is completed. Used for verification checklists.

## Workflow Shortcuts (userTriggered)

These hooks appear as buttons in the Kiro hooks panel. Click to trigger a guided workflow — the Kiro equivalent of Claude Code's slash commands.

| File | Workflow | What It Does |
|------|----------|--------------|
| \`workflow-audit.json\` | Audit | Full governance audit: observe codebase, compare to steering, find drift and dead code |
| \`workflow-new-feature.json\` | New Feature | Spec-first 3-gate workflow: requirements → design → tasks → implement |
| \`workflow-fix.json\` | Fix | Bug diagnosis: reproduce → root cause → minimal fix → regression test |
| \`workflow-refactor.json\` | Refactor | Plan-first refactor: impact analysis gate → tests before → apply → tests after |
| \`workflow-hotfix.json\` | Hotfix | Emergency production fix: smallest change, must have test, safe to deploy |
| \`workflow-explore.json\` | Explore | Read-only codebase exploration: trace data flows, map dependencies |

### How to use

1. Open the **Agent Hooks** panel in Kiro
2. Find the workflow hook (e.g. "New Feature")
3. Click the trigger button
4. The agent will ask what you need and guide you through the workflow

These are equivalent to Claude Code's \`/audit\`, \`/new-feature\`, \`/fix\`, \`/refactor\`, \`/hotfix\`, and \`/explore\` commands.

## Customization

Add your own hooks as JSON files in this directory. Each file must follow the schema:

\`\`\`json
{
  "name": "My Custom Hook",
  "version": "1.0.0",
  "when": { "type": "preToolUse", "toolTypes": ["write"] },
  "then": { "type": "askAgent", "prompt": "Your prompt here" }
}
\`\`\`
`;
}
